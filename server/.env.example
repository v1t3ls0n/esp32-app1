# Copy to server/.env and fill in.
# Generate the VAPID keys with:  npm run gen-keys

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com

# Shared secret the ESP32 must send in the Authorization header. CHANGE THIS.
DEVICE_TOKEN=change-me

# Optional
PORT=3000
