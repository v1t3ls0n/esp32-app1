{
  "name": "esp32-notify",
  "version": "2.0.0",
  "private": true,
  "description": "ESP32 -> Web Push notifications. Vercel serverless functions + installable PWA.",
  "scripts": {
    "gen-keys": "node scripts/generate-vapid.js",
    "dev": "vercel dev"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "web-push": "^3.6.7"
  },
  "engines": {
    "node": ">=18"
  }
}
